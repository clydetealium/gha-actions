name: 'Find Schema Changes'
description: 'Rough check for public schema references'

runs:
  using: 'composite'
  steps:
    - name: Find Schema Changes
      shell: bash
      run: $GITHUB_ACTION_PATH/scripts/pg-schema-check.sh
